# Python requirements for FMU testing
# Install with: pip install -r testing/requirements.txt

# FMU simulation and loading
fmpy>=0.3.30

# Numerical computation
numpy>=1.24.0
scipy>=1.11.0

# Plotting (optional, for visualization)
matplotlib>=3.7.0

# Configuration parsing
tomli>=2.0.0; python_version < '3.11'  # TOML parser (built-in as tomllib in Python 3.11+)

# Testing
pytest>=7.4.0
pytest-xdist>=3.3.0  # Parallel test execution

# FMU compliance checking (if available via pip)
# Note: FMU Checker is typically distributed as a standalone tool
