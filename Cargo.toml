[workspace]
resolver = "2"

members = [
    "crates/*",
    "models/mathematical/*",
    "models/mechanical/*",
    "models/thermal/*",
    "testing/physics-framework",
]

exclude = [
    "models/*/experimental-*",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/freol35241/adml"

[workspace.dependencies]
# FMI export dependencies - migrated to fmi-export from rust-fmi
fmi-export = "0.1"
fmi = "0.6"

# ADML shared crates
adml-solver = { path = "crates/adml-solver" }

# Testing dependencies
approx = "0.5"
criterion = "0.5"

# Packaging utilities
zip = "2.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
clap = { version = "4.0", features = ["derive"] }
anyhow = "1.0"
walkdir = "2.0"
cargo_metadata = "0.18"
chrono = "0.4"

[profile.release]
lto = true
codegen-units = 1
strip = true
opt-level = 3
