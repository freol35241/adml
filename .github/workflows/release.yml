name: Build and Release FMUs

on:
  workflow_dispatch:
    inputs:
      release_name:
        description: 'Release name (e.g., "v1.0.0")'
        required: true
        default: ''
  push:
    tags:
      - 'v*'

env:
  RUST_BACKTRACE: 1
  CARGO_TERM_COLOR: always

jobs:
  # Build FMUs for all models (cross-compile for Linux and Windows)
  build-fmus:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux-x86_64
          - os: windows-latest
            platform: windows-x86_64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install FMU packaging tool
        run: |
          cargo install package_fmu_after_build

      - name: Build all FMUs
        shell: bash
        run: |
          ./scripts/build-all-fmus.sh

      - name: List built FMUs (Linux)
        if: runner.os == 'Linux'
        run: |
          echo "Built FMUs for ${{ matrix.platform }}:"
          ls -lh fmus/*.fmu

      - name: List built FMUs (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          echo "Built FMUs for ${{ matrix.platform }}:"
          ls -lh fmus/*.fmu

      - name: Upload FMU artifacts
        uses: actions/upload-artifact@v4
        with:
          name: fmus-${{ matrix.platform }}
          path: fmus/*.fmu

  # Create GitHub release with all FMUs
  create-release:
    needs: build-fmus
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download Linux FMU artifacts
        uses: actions/download-artifact@v4
        with:
          name: fmus-linux-x86_64
          path: fmus/linux-x86_64/

      - name: Download Windows FMU artifacts
        uses: actions/download-artifact@v4
        with:
          name: fmus-windows-x86_64
          path: fmus/windows-x86_64/

      - name: List downloaded FMUs
        run: |
          echo "📦 FMU files for release:"
          echo ""
          echo "Linux x86_64:"
          ls -lh fmus/linux-x86_64/*.fmu
          echo ""
          echo "Windows x86_64:"
          ls -lh fmus/windows-x86_64/*.fmu

      - name: Determine release details
        id: release-details
        run: |
          if [ "$GITHUB_EVENT_NAME" == "workflow_dispatch" ]; then
            RELEASE_NAME="$RELEASE_INPUT_NAME"
            TAG="$RELEASE_INPUT_NAME"
          else
            TAG="${GITHUB_REF#refs/tags/}"
            RELEASE_NAME="$TAG"
          fi

          if [ -z "$RELEASE_NAME" ]; then
            RELEASE_NAME="release-$(date +%Y-%m-%d)"
            TAG="release-$(date +%Y%m%d-%H%M%S)"
          fi

          echo "name=$RELEASE_NAME" >> $GITHUB_OUTPUT
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "Release: $RELEASE_NAME (tag: $TAG)"
        env:
          RELEASE_INPUT_NAME: ${{ github.event.inputs.release_name }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}

      - name: Generate release notes
        run: |
          cat > release-notes.md << 'EOF'
          # ADML - AI-Generated Dynamical Model Library

          FMI 3.0 compatible FMU files ready to use in any FMI-supporting simulation tool.

          All models are cross-compiled for **Linux x86_64** and **Windows x86_64**.

          ## 📦 Models in this Release

          ### Linux x86_64

          EOF

          # List Linux models with descriptions
          for fmu in fmus/linux-x86_64/*.fmu; do
            model_name=$(basename "$fmu" .fmu)
            echo "- **$model_name.fmu** - FMI 3.0 Co-Simulation model" >> release-notes.md
          done

          cat >> release-notes.md << 'EOF'

          ### Windows x86_64

          EOF

          # List Windows models with descriptions
          for fmu in fmus/windows-x86_64/*.fmu; do
            model_name=$(basename "$fmu" .fmu)
            echo "- **$model_name.fmu** - FMI 3.0 Co-Simulation model" >> release-notes.md
          done

          cat >> release-notes.md << 'EOF'

          ## 🚀 How to Use

          1. Download the FMU file for your desired model and platform
          2. Load it in any FMI 3.0 compatible tool:
             - FMPy (Python)
             - Dymola
             - Simulink with FMI Toolbox
             - OpenModelica
             - And many more...

          ## ✅ Testing

          All FMUs are thoroughly tested with:
          - **Tier 1**: Rust unit & physics tests
          - **Tier 2**: FMU integration tests with FMPy
          - **Tier 3**: FMI 3.0 compliance verification

          ## 📝 Models Included

          - **Dahlquist** - Simple ODE test equation: dx/dt = -k·x
          - **Van der Pol** - Nonlinear oscillator with limit cycle
          - **Bouncing Ball** - Ball with gravity and elastic collisions

          ## 📚 Documentation

          For detailed documentation, visit the [repository](https://github.com/$GITHUB_REPOSITORY).

          ## 🛠️ Technical Details

          - **FMI Version**: 3.0
          - **Mode**: Co-Simulation
          - **Platforms**: Linux x86_64, Windows x86_64
          - **Built with**: Rust + fmu_from_struct

          EOF

          cat release-notes.md
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: ${{ steps.release-details.outputs.name }}
          tag_name: ${{ steps.release-details.outputs.tag }}
          body_path: release-notes.md
          files: |
            fmus/linux-x86_64/*.fmu
            fmus/windows-x86_64/*.fmu
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Summary
        run: |
          echo "## 🎉 Release Created!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Release:** ${{ steps.release-details.outputs.name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** ${{ steps.release-details.outputs.tag }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 FMU Files" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Linux x86_64:**" >> $GITHUB_STEP_SUMMARY
          ls fmus/linux-x86_64/*.fmu | while read fmu; do
            echo "- $(basename $fmu)" >> $GITHUB_STEP_SUMMARY
          done
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Windows x86_64:**" >> $GITHUB_STEP_SUMMARY
          ls fmus/windows-x86_64/*.fmu | while read fmu; do
            echo "- $(basename $fmu)" >> $GITHUB_STEP_SUMMARY
          done
